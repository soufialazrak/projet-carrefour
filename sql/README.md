# Scripts SQL

Ce dossier contient les scripts SQL utilisés dans le projet.

- `init/` : création des tables (couche Silver)
- `gold/` : vues analytiques (couche Gold)
- `cleanup/` : scripts RGPD (anonymisation des données)

Ces scripts permettent de structurer et exploiter les données dans PostgreSQL.